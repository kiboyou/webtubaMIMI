/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  images: {
    domains: ['http://127.0.0.1:8000/'], // Ajoutez ici d'autres domaines si nécessaire
  },
};
module.exports = nextConfig
